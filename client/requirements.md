## Packages
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes
framer-motion | Smooth transitions and layout animations

## Notes
Requires WebSockets on /ws for real-time draft state and player updates.
Tailwind configuration assumption: Tailwind v3 or v4 is set up with standard arbitrary values.
